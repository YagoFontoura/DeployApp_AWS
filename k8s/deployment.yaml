apiVersion: apps/v1
kind: Deployment
metadata:
  name: databasemysql
spec:
  selector:
    matchLabels:
      app: databasemysql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: databasemysql
    spec:
      containers:
      - image: titanpxp/databasemysql:2.1
        name: databasemysql
        ports:
        - containerPort: 3306
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: titanpxp/backend:2.0
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
           memory: "128Mi"
           cpu: "500m"
        ports:
        - containerPort: 8080

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  labels:
    app: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: titanpxp/frontend-application:2.0    
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
           memory: "128Mi"
           cpu: "500m"
        ports:
        - containerPort: 80
          
          

